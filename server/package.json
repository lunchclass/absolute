{
  "name": "absolute",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "pm2 delete all && pm2 restart pm2_config.json"
  },
  "dependencies": {
    "debug": "~2.2.0",
    "express": "~4.14.0",
    "morgan": "~1.7.0",
    "pm2": "latest"
  }
}
